openapi: 3.0.0
info:
  title: Product Management System-api
  version: v1
  contact: {}
  description: "# VAP final assignment\n<hr>\n\n<br>\n\n### Highlights:\n\n<br>\n\n* Respose sends back trouble-shooting steps for user to follow when the face errors.\n<br>\n\n* HATEOS in get request make API more intutive as the responses are informative and self-descriptive.\n<br>\n\n* Successful applied constrains on Product attributes with the help of JOI.\n<br>\n\n* Cachebility in get request that need to be frequently accessed. This increses speed and performance on client-side.\n<br>\n\n* API has almost been implemented.\n<br>\n  \n  \n<br>\n### Please change the url to implemented API \n\n\n\n\n\n\n"
servers:
  - description: My API
    url: 'https://vap-pms.herokuapp.com/'
  - description: SwaggerHub API Auto Mocking
    url: 'https://virtserver.swaggerhub.com/sankalpreddy/sankalp.vap/v1'
paths:
  /admin/register:
    post:
      summary: Register an Administrator
      operationId: post-user-signup
      responses:
        '201':
          description: |-
            ### Created

            `Request Accepted:`
            Adding user as an admin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response_user_reg_success'
              examples:
                success response:
                  value:
                    message: admin registered
                    token: 'bearer <token>'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '409':
          description: |
            ### Conflict
            `Reason:`
            Account already exists

            `Trouble-shoot:`
            Try logging in / use a new email
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response-error'
              examples:
                error response:
                  value:
                    error: account already exists
                    suggestion: try logging in with password
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      description: |
        **Description:**

        This endpoint accepts details of the admin who, is not registered in the system and Then, adds him into the database with the role as administrators.
      tags:
        - register
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Request_user_post'
            examples:
              correct request - 201:
                value:
                  name: Sanchez Oneal
                  email: sanchezoneal@endipine.com
                  password: Shawna123
                  phone_no: 1582583287
                  address:
                    - address: 630 Knapp Street
                      city: Southview
                      state: Georgia
                      pincode: 578902
              wrong request - 400:
                value:
                  name: Burnett Vance
                  email: burnettvance@endipine.com
                  password: Keith123
                  address:
                    - address: 508 Cornelia Street
                      city: Wolcott
                      state: California
                      pincode: 307393
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |name|string|-|sankalp|
          |email|string(email)|-|sankalp@gmail.com|
          |phone_no|number|-|7397440807|
          |password|string|-|password123|
          |address|array(object)|-|-|

          **Address object format**:

          |body parameter|value type|
          |------|------|
          |address|string|
          |city|string|
          |state|string|
          |pincode|number|

          ---
    parameters: []
  /vendor/register:
    post:
      summary: Register a Vendor
      operationId: post-vendor-register
      responses:
        '201':
          description: |-
            ### Created

            `Request Accepted:`
            Adding user as an Vendor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response_user_reg_success'
              examples:
                success response:
                  value:
                    message: vendor registered
                    token: 'bearer <token>'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '409':
          description: |
            ### Conflict
            `Reason:`
            Account already exists

            `Trouble-shoot:`
            Try logging in / use a new email
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response-error'
              examples:
                error response:
                  value:
                    error: account already exists
                    suggestion: try logging in with password
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - register
      description: |-
        **Description:**

        This endpoint accepts details of a vendor, who is not registered in the system and Then adds him into the database with the role as vendor.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Request_user_post'
            examples:
              correct request -201:
                value:
                  name: Stefanie Harvey
                  email: stefanieharvey@endipine.com
                  password: Neal123
                  phone_no: 8914646752
                  address:
                    - address: 450 India Street
                      city: Dorneyville
                      state: Vermont
                      pincode: 330105
              wrong request - 409:
                value:
                  name: Stefanie Harvey
                  email: stefanieharvey@endipine.com
                  password: Neal123
                  phone_no: '8914646752'
                  address:
                    - address: 450 India Street
                      city: Dorneyville
                      state: Vermont
                      pincode: 330105
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |name|string|-|sankalp|
          |email|string(email)|-|sankalp@gmail.com|
          |phone_no|number|-|7397440807|
          |password|string|-|password123|
          |address|array(object)|-|-|

          **Address object format**:

          |body parameter|value type|
          |------|------|
          |address|string|
          |city|string|
          |state|string|
          |pincode|number|

          ---
  /shipper/register:
    post:
      summary: Register a Shipper
      operationId: post-shipper-register
      responses:
        '201':
          description: |-
            ### Created

            `Request Accepted:`
            Adding user as an Shipper
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response_user_reg_success'
              examples:
                success response:
                  value:
                    message: shipper registered
                    token: '<token>'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '409':
          description: |-
            ### Conflict
            `Reason:`
            Account already exists

            `Trouble-shoot:`
            Try logging in / use a new email
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response-error'
              examples:
                error response:
                  value:
                    error: account already exists
                    suggestion: try logging in with password
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - register
      description: |
        **Description:**

        This endpoint accepts details of a Shipper, who is not registered in the system and Then adds him into the database with the role as Shipper.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Request_user_post'
            examples:
              correct request - 201:
                value:
                  name: Ball Bonner
                  email: ballbonner@endipine.com
                  password: Parrish123
                  phone_no: 5282104042
                  address:
                    - address: 430 Harbor Lane
                      city: Coalmont
                      state: District Of Columbia
                      pincode: 982372
              wrong request - 409:
                value:
                  name: Ball Bonner
                  email: ballbonner@endipine.com
                  password: Parrish123
                  phone_no: 5282104042
                  address:
                    - address: 430 Harbor Lane
                      city: Coalmont
                      state: District Of Columbia
                      pincode: '982372'
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |name|string|-|sankalp|
          |email|string(email)|-|sankalp@gmail.com|
          |phone_no|number|-|7397440807|
          |password|string|-|password123|
          |address|array(object)|-|-|

          **Address object format**:

          |body parameter|value type|
          |------|------|
          |address|string|
          |city|string|
          |state|string|
          |pincode|number|

          ---
  /customer/register:
    post:
      summary: Register a Customer
      operationId: post-customer-register
      responses:
        '201':
          description: |-
            ### Created

            `Request Accepted:`
            Adding user as an admin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response_user_reg_success'
              examples:
                success response:
                  value:
                    message: customer registered
                    token: '<token>'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '409':
          description: |
            ### Conflict
            `Reason:`
            Account already exists

            `Trouble-shoot:`
            Try logging in / use a new email
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response-error'
              examples:
                error response:
                  value:
                    error: account already exists
                    suggestion: try logging in with password
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      description: |
        **Description:**

        This endpoint accepts details of the customer who, is not registered in the system and Then, adds him into the database with the role as customer.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Request_user_post'
            examples:
              correct request - 201:
                value:
                  name: Terrell Robbins
                  email: terrellrobbins@endipine.com
                  password: Helen123
                  phone_no: 3020155013
                  address:
                    - address: 879 Rockaway Parkway
                      city: Harrodsburg
                      state: California
                      pincode: 900984
              wrong request - 409:
                value:
                  name: Terrell Robbins
                  email: terrellrobbins@endipine.com
                  password: Helen123
                  phone: 3020155013
                  address:
                    - address: 879 Rockaway Parkway
                      city: Harrodsburg
                      state: California
                      pincode: 900984
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |name|string|-|sankalp|
          |email|string(email)|-|sankalp@gmail.com|
          |phone_no|number|-|7397440807|
          |password|string|-|password123|
          |address|array(object)|-|-|

          **Address object format**:

          |body parameter|value type|
          |------|------|
          |address|string|
          |city|string|
          |state|string|
          |pincode|number|

          ---
      tags:
        - register
  /admin/token:
    post:
      summary: Get token for Administrator
      operationId: post-admin-login
      responses:
        '200':
          description: |-
            ### Accepted

            `Request Accepted:`
            Credential verified. Token generated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response_user_reg_success'
              examples:
                correct response:
                  value:
                    message: success
                    token: 'bearer <token>'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '409':
          description: |
            ### Conflict
            `Reason:`
            Account may not exist/password is wrong

            `Trouble-shoot:`
            Try logging in with correct credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response-error'
              examples:
                Wrong account email:
                  value:
                    error: Account does not exist
                    suggestion: create a new account
                Wrong password:
                  value:
                    error: Password does not match
                    suggestion: please retry with correct password
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - login
      description: |-
        **Description:**

        This endpoint accepts credentials of an admin, who registered in the system in exchange for generating and returning the access token.

        Client-Id: email

        Client-Secret: password
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Request_token_post'
            examples:
              Correct credentials:
                value:
                  email: sanchezoneal@endipine.com
                  password: Shawna123
              Wrong email:
                value:
                  email: sanchezoneal@endipin.com
                  password: Shawna123
              Wrong password:
                value:
                  email: sanchezoneal@endipine.com
                  password: Shawna12
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |email|string(email)|-|sankalp@gmail.com|
          |password|string|-|password123|
    parameters: []
  /customer/token:
    post:
      summary: Get token for Administrator
      operationId: post-customer-login
      responses:
        '200':
          description: |-
            ### Accepted

            `Request Accepted:`
            Credential verified. Token generated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response_user_reg_success'
              examples:
                correct response:
                  value:
                    message: success
                    token: 'bearer <token>'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '409':
          description: |
            ### Conflict
            `Reason:`
            Account may not exist/password is wrong

            `Trouble-shoot:`
            Try logging in with correct credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response-error'
              examples:
                Wrong account email:
                  value:
                    error: Account does not exist
                    suggestion: create a new account
                Wrong password:
                  value:
                    error: Password does not match
                    suggestion: please retry with correct password
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - login
      description: |
        **Description:**

        This endpoint accepts credentials of an customer, who registered in the system in exchange for generating and returning the access token.

        Client-Id: email

        Client-Secret: password
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Request_token_post'
            examples:
              correct request:
                value:
                  email: terrellrobbins@endipine.com
                  password: Helen123
              wrong account email:
                value:
                  email: terrellrobbins@enipine.com
                  password: Helen123
              wrong password:
                value:
                  email: terrellrobbins@endipine.com
                  password: Helen1243
        description: |
          **Body Parameters**: All of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |email|string(email)|-|sankalp@gmail.com|
          |password|string|-|password123|
    parameters: []
  /vendor/token:
    post:
      summary: Get token for Vendor
      operationId: post-vendor-login
      responses:
        '200':
          description: |-
            ### Accepted

            `Request Accepted:`
            Credential verified. Token generated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response_user_reg_success'
              examples:
                correct response:
                  value:
                    message: success
                    token: 'bearer <token>'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '409':
          description: |
            ### Conflict
            `Reason:`
            Account may not exist/password is wrong

            `Trouble-shoot:`
            Try logging in with correct credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response-error'
              examples:
                Wrong account email:
                  value:
                    error: Account does not exist
                    suggestion: create a new account
                Wrong password:
                  value:
                    error: Password does not match
                    suggestion: please retry with correct password
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - login
      description: |
        **Description:**

        This endpoint accepts credentials of an Vendor, who registered in the system in exchange for generating and returning the access token.

        Client-Id: email

        Client-Secret: password
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Request_token_post'
            examples:
              correct request:
                value:
                  email: stefanieharvey@endipine.com
                  password: Neal123
              wrong email address:
                value:
                  email: stefanieharvey@endipin.com
                  password: Neal123
              wrong password:
                value:
                  email: stefanieharvey@endipine.com
                  password: Neal1230
        description: |
          **Body Parameters**: All of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |email|string(email)|-|sankalp@gmail.com|
          |password|string|-|password123|
    parameters: []
  /shipper/token:
    post:
      summary: Get token for Shipper
      operationId: post-shipper-login
      responses:
        '200':
          description: |-
            ### Accepted

            `Request Accepted:`
            Credential verified. Token generated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response_user_reg_success'
              examples:
                correct response:
                  value:
                    message: success
                    token: '<token>'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '409':
          description: |
            ### Conflict
            `Reason:`
            Account may not exist/password is wrong

            `Trouble-shoot:`
            Try logging in with correct credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response-error'
              examples:
                Wrong account email:
                  value:
                    error: Account does not exist
                    suggestion: create a new account
                Wrong password:
                  value:
                    error: Password does not match
                    suggestion: please retry with correct password
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - login
      description: |-
        **Description:**

        This endpoint accepts credentials of an shipper, who registered in the system in exchange for generating and returning the access token.

        Client-Id: email

        Client-Secret: password
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Request_token_post'
            examples:
              correct response:
                value:
                  email: ballbonner@endipine.com
                  password: Parrish123
              wrong email address:
                value:
                  email: ballbonner@endipie.com
                  password: Parrish123
              wrong password:
                value:
                  email: ballbonner@endipine.com
                  password: Parrish1230
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |email|string(email)|-|sankalp@gmail.com|
          |password|string|-|password123|
    parameters: []
  /users:
    get:
      summary: Get the list of users
      tags:
        - user
      responses:
        '200':
          description: |-
            ### OK
            `Request Accepted:`
            The request has been accepted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Request_user_post_id'
                  link:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    data:
                      - _id: 5e746fc181f308458877113d
                        name: Sanchez Oneal
                        email: sanchezoneal@endipine.com
                        password: Shawna123
                        phone_no: 1582583287
                        address:
                          - _id: 5e746fc181f308458877113e
                            address: 630 Knapp Street
                            city: Southview
                            state: Georgia
                            pincode: 578902
                        role: admin
                        __v: 0
                      - _id: 5e7476ba9a0dc94c14f3a60e
                        name: Stefanie Harvey
                        email: stefanieharvey@endipine.com
                        password: Neal123
                        phone_no: 8914646752
                        address:
                          - _id: 5e7476ba9a0dc94c14f3a60f
                            address: 450 India Street
                            city: Dorneyville
                            state: Vermont
                            pincode: 330105
                        role: vendor
                        __v: 0
                      - _id: 5e7476cc9a0dc94c14f3a610
                        name: Ball Bonner
                        email: ballbonner@endipine.com
                        password: Parrish123
                        phone_no: 5282104042
                        address:
                          - _id: 5e7476cc9a0dc94c14f3a611
                            address: 430 Harbor Lane
                            city: Coalmont
                            state: District Of Columbia
                            pincode: 982372
                        role: shipper
                        __v: 0
                      - _id: 5e7476de9a0dc94c14f3a612
                        name: Terrell Robbins
                        email: terrellrobbins@endipine.com
                        password: Helen123
                        phone_no: 3020155013
                        address:
                          - _id: 5e7476de9a0dc94c14f3a613
                            address: 879 Rockaway Parkway
                            city: Harrodsburg
                            state: California
                            pincode: 900984
                        role: customer
                        __v: 0
                    links:
                      - href: 'http://183fd6d9.ngrok.io/users?role=customer'
                        method: GET
                        rel: Get-Customers
                      - href: 'http://183fd6d9.ngrok.io/users?role=admin'
                        method: GET
                        rel: Get-Administrators
                      - href: 'http://183fd6d9.ngrok.io/users?role=shipper'
                        method: GET
                        rel: Get-Shippers
                      - href: 'http://183fd6d9.ngrok.io/users?role=vendors'
                        method: GET
                        rel: Get-Vendors
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-users
      parameters:
        - schema:
            type: string
          in: query
          name: role
          description: param for getting a particular type of user
      security:
        - Authorization: []
      description: "**Description:**\n\nThis endpoint returns a list of users, registered in the system. The list by default includes \n  * administrators\n  * customers\n  * vendors\n  * shippers \n\n\n\n**Query Parameters**:\n\nThese results can be filtered using query parameters.\n\n\n|query parameter|value |result|\n|-----|-----|------|\n|role|admin|List of Administrators|\n||customer|List of Customers|\n||vendor|List of Vendors|\n||shipper|List of Shipping partners|"
    post:
      summary: Add a user
      operationId: post-users
      responses:
        '201':
          description: |-
            ### Created

            `Request Accepted:`
            Adding user to list of Users
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
              examples:
                successful response:
                  value:
                    message: user added
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '409':
          description: "### Conflict\n\n`Request Rejected:`\nAdding user to list of Users is not possible because account already exists "
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
              examples:
                error response:
                  value:
                    error: user already exists
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      description: |-
        **Description:**

        This endpoint accepts a user, who is not registered in the system and Then adds him into the database with any of the following role:
          * administrators (admin)
          * customers (customer)
          * vendors (vendor)
          * shippers (shipper)
      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Model-User'
            examples:
              Correct request format:
                value:
                  name: Annette Burch
                  email: annetteburch@endipine.com
                  password: Barker123
                  phone_no: 7224057891
                  role: vendor
                  address:
                    - address: 731 Cove Lane
                      city: Steinhatchee
                      state: District Of Columbia
                      pincode: 200213
              wrong request format:
                value:
                  name: Annette Burch
                  email: annetteburch@endipine.com
                  password: Barker123
                  phone_no: '7224057891'
                  role: vendor
                  address:
                    - address: 731 Cove Lane
                      city: Steinhatchee
                      state: District Of Columbia
                      pincode: 200213
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |name|string|-|sankalp|
          |email|string(email)|-|sankalp@gmail.com|
          |phone_no|number|-|7397440807|
          |password|string|-|password123|
          |address|object|-|-|
          |role|string|admin|-|
          |||customer|-|
          |||vendor|-|
          |||shipper|-|

          **Address format**:

          |body parameter|value type|
          |------|------|
          |address|string|
          |city|string|
          |state|string|
          |pincode|number|
      tags:
        - user
  '/users/{userid}':
    parameters:
      - schema:
          type: string
        name: userid
        in: path
        required: true
    get:
      summary: Get a selected user
      tags:
        - user
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning the requested User details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Request_user_post_id'
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    data:
                      _id: 5e746fc181f308458877113d
                      name: Sanchez Oneal
                      email: sanchezoneal@endipine.com
                      password: Shawna123
                      phone_no: 1582583287
                      address:
                        - _id: 5e746fc181f308458877113e
                          address: 630 Knapp Street
                          city: Southview
                          state: Georgia
                          pincode: 578902
                      role: admin
                      __v: 0
                    links:
                      - href: 'http://183fd6d9.ngrok.io/users/5e746fc181f308458877113d'
                        method: PUT
                        rel: Update-user
                      - href: 'http://183fd6d9.ngrok.io/users/5e746fc181f308458877113d'
                        method: PATCH
                        rel: Update-user-field
                      - href: 'http://183fd6d9.ngrok.io/users/5e746fc181f308458877113d'
                        method: DELETE
                        rel: Delete-user
                      - href: 'http://183fd6d9.ngrok.io/users/5e746fc181f308458877113d/address'
                        method: GET
                        rel: "Get-user's-list-of-addresses"
                      - href: 'http://183fd6d9.ngrok.io/users/5e746fc181f308458877113d/address'
                        method: POST
                        rel: "Add-address-to-user's-list-of-addresses"
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |-
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
            Server Error
      operationId: get-user-userid
      description: |
        **Description:**

        This endpoint returns a single user and his details. The user is selected based on his unique id passed in the path parameters.

        **Path Parameters**:

        |path parameter|value |
        |-----|-----|
        |userId|<user_id>|
      security:
        - Authorization: []
    put:
      summary: Update a Selected User
      operationId: put-user-userid
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected user. No Response has been included for greater responsiveness and speed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response_user_reg_success'
              examples: {}
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - user
      security:
        - Authorization: []
      description: "**Description:** \n\nThis endpoint accepts user details of a user, who is already registered in the system with the help of the user id provided in the path parameters and Then updates all of his details mentioned in the request body.\n\n"
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Model-User'
            examples:
              Correct format:
                value:
                  name: Annette Burch
                  email: annetteburch@endipine.com
                  password: Barker123
                  phone_no: 7224057891
                  role: vendor
                  address:
                    - address: 731 Cove Lane
                      city: Steinhatchee
                      state: District Of Columbia
                      pincode: 200213
              wrong format:
                value:
                  name: Annette Burch
                  email: annetteburch@endipine.com
                  password: Barker123
                  phone_no: '7224057891'
                  role: vendor
                  address:
                    - address: 731 Cove Lane
                      city: Steinhatchee
                      state: District Of Columbia
                      pincode: 200213
        description: |-
          **Body Parameters**: One or More of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |name|string|-|sankalp|
          |email|string(email)|-|sankalp@gmail.com|
          |phone_no|number|-|7397440807|
          |password|string|-|password123|
          |address|object|-|-|
          |role|string|admin|-|
          |||customer|-|
          |||vendor|-|
          |||shipper|-|


          **Address format**:

          |body parameter|value type|
          |------|------|
          |address|string|
          |city|string|
          |state|string|
          |pincode|number|
    patch:
      summary: "Update a Selected User's Details"
      operationId: patch-user-userid
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected user details. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - user
      security:
        - Authorization: []
      description: |-
        **Description:**

        This endpoint accepts user details of a user, who is already registered in the system with the help of the user id provided in the path parameters and Then updates one or more of his details mentioned in the request body.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Reques_user_put_optional'
            examples:
              correct format:
                value:
                  address:
                    - address: 731 Cove Lane
                      city: Steinhatchee
                      state: District Of Columbia
                      pincode: 200213
              wrong format:
                value:
                  address:
                    - address: 731 Cove Lane
                      city: Steinhatchee
                      pincode: 200213
              correct format 1:
                value:
                  name: mark antony
        description: |-
          **Body Parameters**: One or more of the following

          |body parameter|value type|options|example|
          |------|------|------|------|
          |name|string|-|sankalp|
          |email|string(email)|-|sankalp@gmail.com|
          |phone_no|number|-|7397440807|
          |password|string|-|password123|
          |address|object|-|-|
          |role|string|admin|-|
          |||customer|-|
          |||vendor|-|
          |||shipper|-|


          **Address format**: full object or nothing

          |body parameter|value type|
          |------|------|
          |address|string|
          |city|string|
          |state|string|
          |pincode|number|
    delete:
      summary: Delete a Selected Users
      operationId: delete-user-userid
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Deleted selected user. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - user
      security:
        - Authorization: []
      description: "**Description:** \n\nThis endpoint accepts user details of a user, who is already registered in the system with the help of the user id provided in the path parameters and Then deletes the user from the database.\n\n\n**Query Parameters**:\n\nnone\n\n**Body Parameters**:\n\nnone"
  '/users/{userId}/address':
    parameters:
      - schema:
          type: string
        name: userId
        in: path
        required: true
    get:
      summary: 'Get List of, Addresses of, Selected User'
      tags:
        - user address
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning the requested list of addresses of User.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        address:
                          type: string
                        city:
                          type: string
                        state:
                          type: string
                        pincode:
                          type: number
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                Successful response:
                  value:
                    data:
                      - _id: 5e746fc181f308458877113e
                        address: 630 Knapp Street
                        city: Southview
                        state: Georgia
                        pincode: 578902
                    links:
                      - href: 'http://183fd6d9.ngrok.io/users/5e746fc181f308458877113d/address'
                        method: POST
                        rel: "Add-address-to-user's-list-of-addresses"
                      - href: 'http://183fd6d9.ngrok.io/users/5e746fc181f308458877113d/address/<address ID>'
                        method: GET
                        rel: GET-a-particular-address-of-a-user
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-users-userId-address
      description: |
        **Description:**

        This endpoint returns a single user's address details in an array. The user is selected based on his unique id passed in the path parameters.

        **Path Parameters**:

        |path parameter|value |
        |-----|-----|
        |userId|<user_id>|
      security:
        - Authorization: []
    post:
      summary: "Add an Address to User's list of Addresses"
      operationId: post-users-userId-address
      responses:
        '201':
          description: |-
            `Request Accepted:`
            Adding new address to list of address for the selected user.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - user address
      description: "**Description:**\n\nThis endpoint accepts an Address, for a particular user selected based on the user id provided in the path and adds the address to the list of address. \n\n**Path Parameters**:\n\n|path parameter|value |\n|-----|-----|\n|userId|<user_id>|\n\n**Query Parameters**:\n\nnone\n\n\n**Body Parameters**: All of the following\n\n|body parameter|value type|\n|------|------|\n|address|string|\n|city|string|\n|state|string|\n|pincode|number|"
      security:
        - Authorization: []
  '/users/{userId}/address/{addressId}':
    parameters:
      - schema:
          type: string
        name: userId
        in: path
        required: true
      - schema:
          type: string
        name: addressId
        in: path
        required: true
    get:
      summary: Get a Particular Address Of a Particular User
      tags:
        - user address
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning the requested address of selected user
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      address:
                        type: string
                      city:
                        type: string
                      state:
                        type: string
                      pincode:
                        type: number
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    data:
                      _id: 5e746fc181f308458877113e
                      address: 630 Knapp Street
                      city: Southview
                      state: Georgia
                      pincode: 578902
                    links:
                      - href: 'http://183fd6d9.ngrok.io/users/5e746fc181f308458877113d/address/5e746fc181f308458877113e'
                        method: PUT
                        rel: update-a-particular-address-of-a-user
                      - href: 'http://183fd6d9.ngrok.io/users/5e746fc181f308458877113d/address/5e746fc181f308458877113e'
                        method: PATCH
                        rel: update-a-field-of-a-particular-address-of-a-user
                      - href: 'http://183fd6d9.ngrok.io/users/5e746fc181f308458877113d/address/5e746fc181f308458877113e'
                        method: DELETE
                        rel: Delete-a-particular-address-of-a-user
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |-
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
            Server Error
      operationId: get-users-userId-address-addressId
      description: |-
        **Description:**

        This endpoint returns a single user's select address details. The user  and the address are selected based on his unique id and address is passed in the path parameters.

        **Path Parameters**:

        |path parameter|value |
        |-----|-----|
        |userId|<user_id>|
        |addressId|<address_id>|

        **Query Parameters**:

        none
      security:
        - Authorization: []
    put:
      summary: "Update an Address from the User's list of Addresses"
      operationId: put-users-userId-address-addressId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected address. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - user address
      description: "**Description:**\n\nThis endpoint accepts an Address, using the user_id and address_id provided in the path and updates the address in the list of address of a particular address. \n\n**Path Parameters**:\n\n|path parameter|value |\n|-----|-----|\n|userId|<user_id>|\n|addressId|<address_id>|\n\n**Query Parameters**:\n\nnone\n\n\n**Body Parameters**: All the following\n\n|body parameter|value type|\n|------|------|\n|address|string|\n|city|string|\n|state|string|\n|pincode|number|"
      security:
        - Authorization: []
    patch:
      summary: "Update an Address's field from the User's list of Addresses"
      operationId: patch-users-userId-address-addressId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected address field. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - user address
      description: |-
        **Description:**

        This endpoint accepts address details of a user, who is already registered in the system with the help of the user_id and address_id provided in the path parameters and then updates one or more of his address details mentioned in the request body.


        **Query Parameters**:

        none

        **Body Parameters**: One or more of the following

        |body parameter|value type|
        |------|------|
        |address|string|
        |city|string|
        |state|string|
        |pincode|number|
      security:
        - Authorization: []
    delete:
      summary: "Delete an Address from the User's list of Addresses"
      operationId: delete-users-userId-address-addressId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Deleted selected address field. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - user address
      description: "**Description:** \n\nThis endpoint selects an address with the help of the user_id and address_id provided in the path parameters and Then deletes the address from the database.\n\n\n**Query Parameters**:\n\nnone\n\n**Body Parameters**:\n\nnone"
      security:
        - Authorization: []
  /categories:
    get:
      summary: Get List Of Available Node Categories
      tags:
        - categories
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning List of Category Schemas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        category_schema:
                          type: object
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    data:
                      - _id: 5e70fac1175f0e1a1060ed3e
                        name: formal pants
                        category_schema:
                          waist_in_inches:
                            type: number
                            required: true
                          length_in_inches:
                            type: number
                            required: true
                          color:
                            type: string
                            required: true
                        __v: 0
                      - _id: 5e70fd757661a913941a2c60
                        name: Mobile Phones
                        category_schema:
                          ram_gb:
                            type: number
                            required: true
                          battery_capacity_mah:
                            type: number
                            required: true
                          dimensions:
                            type: string
                            required: true
                        __v: 0
                    links:
                      - href: 'http://183fd6d9.ngrok.io/categories'
                        method: POST
                        rel: Add-new-category
                      - href: 'http://183fd6d9.ngrok.io/categories/{categoryId}'
                        method: GET
                        rel: Get-a-particular-categories
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-categories
      description: "**Description:**\n\nThis endpoint returns a list of category schemas, saved in the system by the Administrator. \n\nThe vendor when adding attributes to the product must conform to the schema other-wise the attributes will be rejected."
    post:
      summary: Add a Category Schema Which the Vendor Can Pick
      operationId: post-categories
      responses:
        '201':
          description: |
            ### Created

            `Category Created:`New category is created
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
              examples:
                successful response:
                  value:
                    message: category added
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '409':
          description: "### Conflict\n\n`Request Rejected:`\nAdding category to list of categories is not possible because category already exists "
          content:
            application/json:
              schema:
                type: object
                properties:
                  erreor:
                    type: string
              examples:
                error message:
                  value:
                    error: category already exists
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - categories
      description: |-
        **Description:**

        This endpoint accepts the node category name and the json_schema which can be converted into a JOI object for validation of attributes.
      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                category_schema:
                  type: object
            examples:
              correct format:
                value:
                  name: Mobile Phone
                  category_schema:
                    ram_gb:
                      type: number
                      required: true
                    battery_capacity:
                      type: number
                      required: true
                    dimensions:
                      type: string
                      required: true
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|example|
          |------|------|------|
          |name|string|Mobile Phones|
          |category_schema|object|-|


          **category_schema format**:

          |body parameter|keys|possible values|
          |------|------|------|
          |field_name|type|string|
          |||number|
          ||required|true|
          |||false|
  '/categories/{categoryId}':
    parameters:
      - schema:
          type: string
        name: categoryId
        in: path
        required: true
    get:
      summary: GET a Particular Category
      tags:
        - categories
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returns a Category Schemas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      category_schema:
                        type: object
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    data:
                      _id: 5e70fac1175f0e1a1060ed3e
                      name: formal pants
                      category_schema:
                        waist_in_inches:
                          type: number
                          required: true
                        length_in_inches:
                          type: number
                          required: true
                        color:
                          type: string
                          required: true
                      __v: 0
                    links: []
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-categories-categoryId
      description: "**Description:**\n\nThis endpoint returns a particular category schemas, saved in the system by the Administrator. \n\nThis Category is picked with the help of the category_id passed as an path parameter.\n\nThe vendor when adding attributes to the product must conform to the schema other-wise the attributes will be rejected."
    put:
      summary: Update a Entire Category
      operationId: put-categories-categoryId
      responses:
        '204':
          description: |-
            ### Action Performed

            `Request Accepted:`
            Category Schema updated
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - categories
      description: |-
        **Description:**

        This endpoint accepts the node category name and the json_schema which can be converted into a JOI object for validation of attributes.

        Then it updates an existing category which is identified using the categoryId
      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                category_schema:
                  type: object
            examples:
              correct format:
                value:
                  name: Mobile Phone
                  category_schema:
                    ram_gb:
                      type: number
                      required: true
                    battery_capacity:
                      type: number
                      required: true
                    dimensions:
                      type: string
                      required: true
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|example|
          |------|------|------|
          |name|string|Mobile Phones|
          |category_schema|object|-|


          **category_schema format**:

          |body parameter|keys|possible values|
          |------|------|------|
          |field_name|type|string|
          |||number|
          ||required|true|
          |||false|
    patch:
      summary: Update a Particular Category Field
      operationId: patch-categories-categoryId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected user details. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - categories
      description: |-
        **Description:**

        This endpoint accepts neew category details for a category in the system with the help of the category_id provided in the path parameters and Then updates one or more of it's details mentioned in the request body.
      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                category_schema:
                  type: object
            examples:
              correct format 1:
                value:
                  name: Mobile Phone
                  category_schema:
                    ram_gb:
                      type: number
                      required: true
                    battery_capacity:
                      type: number
                      required: true
                    dimensions:
                      type: string
                      required: true
              correct format 2:
                value:
                  category_schema:
                    ram_gb:
                      type: number
                      required: true
                    battery_capacity:
                      type: number
                      required: true
                    dimensions:
                      type: string
                      required: true
              correct format 3:
                value:
                  name: Mobile Phone
              wrong format:
                value:
                  other: Mobile Phone
        description: |-
          **Body Parameters**: One or More of the following

          |body parameter|value type|example|
          |------|------|------|
          |name|string|Mobile Phones|
          |category_schema|object|-|


          **category_schema format**: All of the following

          |body parameter|keys|possible values|
          |------|------|------|
          |field_name|type|string|
          |||number|
          ||required|true|
          |||false|
    delete:
      summary: Delete a Particular Category Field
      operationId: delete-categories-categoryId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Deleted selected user. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - categories
      description: "**Description:** \n\nThis endpoint pinpoints the category, which is already saved in the system with the help of the category_id provided in the path parameters and Then deletes the category from the database.\n\n\n**Query Parameters**:\n\nnone\n\n**Body Parameters**:\n\nnone"
      security:
        - Authorization: []
  /L2-categories:
    get:
      summary: Get list Level 2 Categories
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning List of Users
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        sub_categories:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                type: string
                              name:
                                type: string
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    data:
                      - sub_categories:
                          - _id: 5e70fd757661a913941a2c60
                            name: Mobile Phones
                            category_schema:
                              ram_gb:
                                type: number
                                required: true
                              battery_capacity_mah:
                                type: number
                                required: true
                              dimensions:
                                type: string
                                required: true
                            __v: 0
                        _id: 5e70fdcd7661a913941a2c61
                        name: Phones and Tablets
                    links:
                      - href: 'http://183fd6d9.ngrok.io/categories'
                        method: get
                        rel: get-all-categories
                      - href: 'http://183fd6d9.ngrok.io/categories/{categoryId}'
                        method: get
                        rel: get-particular-category
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-L2-categories
      tags:
        - L2-category
      description: "**Description:**\n\nThis endpoint returns a list of all Level 2 categories along with their sub_categories, saved in the system by the Administrator/Vendor. \n"
    post:
      summary: Create a new L2-category
      operationId: post-L2-categories
      tags:
        - L2-category
      description: |+
        **Description:**

        This endpoint accepts the Level 2 category name and the array of node categories and then creates a new Level 2 category with the array of node categories as sub_categories.



      security:
        - Authorization: []
      responses:
        '201':
          description: |
            ### Created

            `Category Created:`New category is created
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
              examples:
                successful response:
                  value:
                    message: l2_category added
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '409':
          description: "### Conflict\n\n`Request Rejected:`\nAdding category to list of categories is not possible because category already exists "
          content:
            application/json:
              schema:
                type: object
                properties:
                  erreor:
                    type: string
              examples:
                error message:
                  value:
                    error: l2_category already exists
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                sub_categories:
                  type: array
                  items:
                    type: string
            examples:
              correct format:
                value:
                  name: Pants
                  sub_categories:
                    - 5e70fac1175f0e1a1060ed3e
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|
          |------|------|
          |name|string|
          |sub_categories|array { string(category ids) }|
  '/L2-categories/{l2_categoryId}':
    parameters:
      - schema:
          type: string
        name: l2_categoryId
        in: path
        required: true
    get:
      summary: GET a particular L2-category
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning List of Users
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      sub_categories:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            name:
                              type: string
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    sub_categories:
                      - _id: 5e70fd757661a913941a2c60
                        name: Mobile Phones
                    _id: 5e70fdcd7661a913941a2c61
                    name: Phones and Tablets
                    __v: 0
          headers: {}
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-L2-categories-l2-categoryId
      tags:
        - L2-category
      description: "**Description:**\n\nThis endpoint returns a particular Level 2 categories, saved in the system by the Administrator/Vendor. \n"
    put:
      summary: Update a particular L2-category
      operationId: put-L2-categories-l2_categoryId
      tags:
        - L2-category
      description: |-
        **Description:**

        This endpoint accepts the Level 2 category name and the array of node categories and then updates a particular Level 2 category with the array of node categories as sub_categories.

        The Level 2 category is selected with the help of l2_categoryId passed through the path parameters.
      security:
        - Authorization: []
      responses:
        '204':
          description: |-
            ### Action Performed

            `Request Accepted:`
            l2_Category Schema updated
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                sub_categories:
                  type: array
                  items:
                    type: string
            examples:
              correct format:
                value:
                  name: Pants
                  sub_categories:
                    - 5e70fac1175f0e1a1060ed3e
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|
          |------|------|
          |name|string|
          |sub_categories|array { string(category ids) }|
    patch:
      summary: "Update a particular L2-category's field"
      operationId: patch-L2-categories-l2_categoryId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected l2_category. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - L2-category
      description: |-
        **Description:**

        This endpoint accepts the Level 2 category name or the array of node categories and then updates a particular field either the category name or the array of node categories which are the sub_categories.

        The Level 2 category is selected with the help of l2_categoryId passed through the path parameters.
      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                sub_categories:
                  type: array
                  items:
                    type: string
            examples:
              correct format 1:
                value:
                  name: Pants
                  sub_categories:
                    - 5e70fac1175f0e1a1060ed3e
              correct format 2:
                value:
                  name: Pants
                  sub_categories:
                    - 5e70fac1175f0e1a1060ed3e
              correct format 3:
                value:
                  sub_categories:
                    - 5e70fac1175f0e1a1060ed3e
        description: |-
          **Body Parameters**: One or More of the following

          |body parameter|value type|
          |------|------|
          |name|string|
          |sub_categories|array { string(category ids) }|
    delete:
      summary: DELETE a particular L2-category
      operationId: delete-L2-categories-l2_categoryId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Deleted selected l2_category. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - L2-category
      description: "**Description:** \n\nThis endpoint pinpoints the Level-2-category, which is already saved in the system with the help of the l2_category_id provided in the path parameters and then deletes the l2_category along with its sub_categories from the database.\n\n\n**Query Parameters**:\n\nnone\n\n**Body Parameters**:\n\nnone"
      security:
        - Authorization: []
  /L1-categories:
    get:
      summary: Get list Level 1 Categories
      tags:
        - L1-category
      operationId: get-L1-categories
      description: "**Description:**\n\nThis endpoint returns a list of all Level 1 categories along with their sub_categories, saved in the system by the Administrator/Vendor. \n"
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning List of Users
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        sub_categories:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                type: string
                              name:
                                type: string
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    data:
                      - sub_categories:
                          - _id: 5e70fd757661a913941a2c60
                            name: Mobile Phones
                            category_schema:
                              ram_gb:
                                type: number
                                required: true
                              battery_capacity_mah:
                                type: number
                                required: true
                              dimensions:
                                type: string
                                required: true
                            __v: 0
                        _id: 5e70fdcd7661a913941a2c61
                        name: Phones and Tablets
                    links:
                      - href: 'http://183fd6d9.ngrok.io/categories'
                        method: get
                        rel: get-all-categories
                      - href: 'http://183fd6d9.ngrok.io/categories/{categoryId}'
                        method: get
                        rel: get-particular-category
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
    post:
      summary: Create a new L1-category
      operationId: post-L1-categories
      tags:
        - L1-category
      description: |-
        **Description:**

        This endpoint accepts the node category name and the json_schema which can be converted into a JOI object for validation of attributes.
      security:
        - Authorization: []
      responses:
        '201':
          description: |
            ### Created

            `Category Created:`New category is created
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
              examples:
                successful response:
                  value:
                    message: category added
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '409':
          description: "### Conflict\n\n`Request Rejected:`\nAdding category to list of categories is not possible because category already exists "
          content:
            application/json:
              schema:
                type: object
                properties:
                  erreor:
                    type: string
              examples:
                error message:
                  value:
                    error: category already exists
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                sub_categories:
                  type: array
                  items:
                    type: string
            examples:
              correct format:
                value:
                  name: Pants
                  sub_categories:
                    - 5e70fac1175f0e1a1060ed3e
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|example|
          |------|------|------|
          |name|string|Mobile Phones|
          |category_schema|object|-|


          **category_schema format**:

          |body parameter|keys|possible values|
          |------|------|------|
          |field_name|type|string|
          |||number|
          ||required|true|
          |||false|
  '/L1-categories/{l1_categoryId}}':
    parameters:
      - schema:
          type: string
        name: l1_categoryId
        in: path
        required: true
    get:
      summary: GET a particular L1-category
      tags:
        - L1-category
      operationId: get-L1-categories-l1_categoryId
      description: "**Description:**\n\nThis endpoint returns a particular Level 1 categories, saved in the system by the Administrator/Vendor. \n"
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning List of Users
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      sub_categories:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            name:
                              type: string
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    sub_categories:
                      - _id: 5e70fd757661a913941a2c60
                        name: Mobile Phones
                    _id: 5e70fdcd7661a913941a2c61
                    name: Phones and Tablets
                    __v: 0
          headers: {}
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
    put:
      summary: Update a particular L1-category
      operationId: put-L1-categories-l1_categoryId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected l1_category. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - L1-category
      description: |-
        **Description:**

        This endpoint accepts the Level 1 category name and the array of Level 2 categories id and then updates a particular Level 1 category with the array of Level 2 categories as sub_categories.

        The Level 1 category is selected with the help of l1_categoryId passed through the path parameters.
      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                sub_categories:
                  type: array
                  items:
                    type: string
                name:
                  type: string
            examples:
              correct format:
                value:
                  name: Pants
                  sub_categories:
                    - 5e70fac1175f0e1a1060ed3e
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|
          |------|------|
          |name|string|
          |sub_categories|array { string(l2 category ids) }|
    patch:
      summary: "Update a particular L1-category's field"
      operationId: patch-L1-categories-l1_categoryId
      tags:
        - L1-category
      description: |-
        **Description:**

        This endpoint accepts the Level 1 category name or the array of Level 2 categories and then updates a particular field either the category name or the array of Level 2 categories which are the sub_categories.

        The Level 1 category is selected with the help of l1_categoryId passed through the path parameters.
      security:
        - Authorization: []
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected l1_category details. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                sub_categories:
                  type: array
                  items:
                    type: string
            examples:
              correct format:
                value:
                  name: Pants
                  sub_categories:
                    - 5e70fac1175f0e1a1060ed3e
        description: |-
          **Body Parameters**: One or More of the following

          |body parameter|value type|
          |------|------|
          |name|string|
          |sub_categories|array { string(l2 category ids) }|
    delete:
      summary: DELETE a particular L1-category
      operationId: delete-L1-categories-l1_categoryId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Deleted selected l1_category. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - L1-category
      description: "**Description:** \n\nThis endpoint pinpoints the Level-1-category, which is already saved in the system with the help of the l1_category_id provided in the path parameters and then deletes the l1_category along with its sub_categories from the database."
      security:
        - Authorization: []
  /products:
    get:
      summary: GET List Of Products
      tags:
        - products
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning List of products
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Model-Product'
                  link:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    data:
                      - images:
                          - 'https://images.samsung.com/is/image/samsung/hk-en-galaxy-a71-a715-sm-a715fzsgtgy-silver-205335154?$PD_GALLERY_L_JPG$'
                        _id: 5e71dd371927723aa8c93e97
                        product_name: Samsung A71
                        product_description: A powerful smart phone with excelent hardware at a reasonable price
                        brand: Samsung
                        price: 27199
                        cost_price: 20000
                        mrp: 30000
                        currency: rupees
                        vendor: 5e71cb849b32af35b44155d5
                        category_schema: 5e70fd757661a913941a2c60
                        __v: 0
                        product_attributes:
                          ram_gb: 4
                          battery_capacity_mah: 5000
                          dimensions: 25 mm x 45 mm x 10 mm
                    links:
                      - href: 'http://183fd6d9.ngrok.io/products/{productId}'
                        method: get
                        rel: get-particular-product
                      - href: 'http://183fd6d9.ngrok.io/products/{productId}/attributes'
                        method: get
                        rel: "get-particular-product's-attributes"
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-products
      description: "**Description:**\n\nThis endpoint returns a list of all product details along with the category and attributes details that are saved in the system by the Administrator/Vendor. "
      parameters:
        - schema:
            type: string
            enum:
              - color
              - brand
          in: query
          name: filter-by
        - schema:
            type: string
            enum:
              - price
              - cost_price
          in: query
          name: sort-by
        - schema:
            type: string
          in: query
          name: filter
          description: all small leters
        - schema:
            type: integer
          in: query
          name: sort
          description: 1 for assending -1 for decending
    post:
      summary: Add a new product
      operationId: post-products
      responses:
        '201':
          description: |-
            ### Created

            `Request Accepted:`
            Product created
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '403':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - products
      description: |-
        **Description:**

        This endpoint accepts the product details and then adds it into the database.
      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                product_name:
                  type: string
                product_description:
                  type: string
                brand:
                  type: string
                price:
                  type: number
                cost_price:
                  type: number
                mrp:
                  type: number
                currency:
                  type: string
                vendor:
                  type: string
                category_schema:
                  type: string
                product_attributes:
                  type: object
                images:
                  type: array
                  items:
                    type: string
            examples:
              correct format:
                value:
                  images:
                    - 'https://images.samsung.com/is/image/samsung/hk-en-galaxy-a71-a715-sm-a715fzsgtgy-silver-205335154?$PD_GALLERY_L_JPG$'
                  product_name: Samsung A71
                  product_description: A powerful smart phone with excelent hardware at a reasonable price
                  brand: Samsung
                  price: 27199
                  cost_price: 20000
                  mrp: 30000
                  currency: rupees
                  vendor: 5e71cb849b32af35b44155d5
                  category_schema: 5e70fd757661a913941a2c60
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|
          |------|------|
          |product_name|String|
          |product_description|String|
          |brand|String|
          |price|Number|
          |cost_price|Number|
          |mrp|Number|
          |currency|String|
          |vendor|String(id)|
          |category_schema|String(id)|
          |images| array{String(url)} |
          |product_attributes|object|


          **Products_attribute**: `Structure of object is determined by category schema`

          check the endpoint `/product/{productId}/attributes` for more details
  '/products/{productId}':
    parameters:
      - schema:
          type: string
        name: productId
        in: path
        required: true
    get:
      summary: Get a particular Product
      tags:
        - products
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning List of Products
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Model-Product'
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/Links'
              examples:
                successful response:
                  value:
                    data:
                      images:
                        - 'https://images.samsung.com/is/image/samsung/hk-en-galaxy-a71-a715-sm-a715fzsgtgy-silver-205335154?$PD_GALLERY_L_JPG$'
                      _id: 5e71dd371927723aa8c93e97
                      product_name: Samsung A71
                      product_description: A powerful smart phone with excelent hardware at a reasonable price
                      brand: Samsung
                      price: 27199
                      cost_price: 20000
                      mrp: 30000
                      currency: rupees
                      vendor: 5e71cb849b32af35b44155d5
                      category_schema: 5e70fd757661a913941a2c60
                      __v: 0
                      product_attributes:
                        ram_gb: 4
                        battery_capacity_mah: 5000
                        dimensions: 25 mm x 45 mm x 10 mm
                    links:
                      - href: 'http://183fd6d9.ngrok.io/products/5e71dd371927723aa8c93e97/attributes'
                        method: get
                        rel: "get-product's-attributes"
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-products-productId
      description: "**Description:**\n\nThis endpoint returns a particular product details along with the category and attributes details that are saved in the system by the Administrator/Vendor. "
    put:
      summary: Update a particular Product
      operationId: put-products-productId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected product. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - products
      description: |
        **Description:**

        This endpoint accepts all the product details and then updates product in the database.

        It pinpoints the product with the help of the product_id passed through the path parameters.
      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                images:
                  type: array
                  items:
                    type: string
                product_attributes:
                  type: object
                category_schema:
                  type: string
                vendor:
                  type: string
                currency:
                  type: string
                mrp:
                  type: number
                cost_price:
                  type: number
                price:
                  type: number
                brand:
                  type: string
                product_description:
                  type: string
                product_name:
                  type: string
            examples:
              correct format:
                value:
                  images:
                    - 'https://images.samsung.com/is/image/samsung/hk-en-galaxy-a71-a715-sm-a715fzsgtgy-silver-205335154?$PD_GALLERY_L_JPG$'
                  product_name: Samsung A71
                  product_description: A powerful smart phone with excelent hardware at a reasonable price
                  brand: Samsung
                  price: 27199
                  cost_price: 20000
                  mrp: 30000
                  currency: rupees
                  vendor: 5e71cb849b32af35b44155d5
                  category_schema: 5e70fd757661a913941a2c60
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|
          |------|------|
          |product_name|String|
          |product_description|String|
          |brand|String|
          |price|Number|
          |cost_price|Number|
          |mrp|Number|
          |currency|String|
          |vendor|String(id)|
          |category_schema|String(id)|
          |images| array{String(url)} |
          |product_attributes|object|


          **Products_attribute**: `Structure of object is determined by category schema`

          check the endpoint `/product/{productId}/attributes` for more details
    patch:
      summary: Update a particular Product Field
      operationId: patch-products-productId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected product details. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - products
      description: |-
        **Description:**

        This endpoint accepts One or More of the product details and then updates product details in the database.

        It pinpoints the product with the help of the product_id passed through the path parameters.
      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                product_name:
                  type: string
                product_description:
                  type: string
                brand:
                  type: string
                price:
                  type: number
                cost_price:
                  type: number
                mrp:
                  type: number
                currency:
                  type: string
                vendor:
                  type: string
                category_schema:
                  type: string
                product_attributes:
                  type: object
                images:
                  type: array
                  items:
                    type: string
            examples:
              correct format:
                value:
                  images:
                    - 'https://images.samsung.com/is/image/samsung/hk-en-galaxy-a71-a715-sm-a715fzsgtgy-silver-205335154?$PD_GALLERY_L_JPG$'
                  mrp: 30000
                  currency: rupees
                  vendor: 5e71cb849b32af35b44155d5
                  category_schema: 5e70fd757661a913941a2c60
        description: |-
          **Body Parameters**: One or More of the following

          |body parameter|value type|
          |------|------|
          |product_name|String|
          |product_description|String|
          |brand|String|
          |price|Number|
          |cost_price|Number|
          |mrp|Number|
          |currency|String|
          |vendor|String(id)|
          |category_schema|String(id)|
          |images| array{String(url)} |
          |product_attributes|object|


          **Products_attribute**: `Structure of object is determined by category schema`

          check the endpoint `/product/{productId}/attributes` for more details
    delete:
      summary: Delete a particular Product
      operationId: delete-products-productId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Deleted selected product. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - products
      description: "**Description:** \n\nThis endpoint pinpoints the product, which is already saved in the system with the help of the product_id provided in the path parameters and then deletes the product from the database.\n\n\n**Query Parameters**:\n\nnone\n\n**Body Parameters**:\n\nnone"
      security:
        - Authorization: []
  '/products/{productId}/attributes':
    parameters:
      - schema:
          type: string
        name: productId
        in: path
        required: true
    get:
      summary: Get Attributes of a Particular Product
      tags:
        - attributes
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning Atributes of products
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-products-productId-attributes
      description: "**Description:**\n\nThis endpoint returns a particular product's attributes details that are saved in the system by the Administrator/Vendor. "
    post:
      summary: Add Attributes to a Particular Product
      operationId: post-products-productId-attributes
      responses:
        '201':
          description: |
            ### Created
            `Request Accepted:`
            Attributes created
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '403':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - attributes
      description: |
        **Description:**

        This endpoint accepts all of the product attribute details and then adds it into the product attributes field in the database.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties: {}
        description: '**Body Parameters**: `Determined by the category schema`'
      security:
        - Authorization: []
    put:
      summary: Update Attributes of a Particular Product
      operationId: put-products-productId-attributes
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected product's attributes. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - attributes
      description: |+
        **Description:**

        This endpoint accepts all the product attribute details and then updates the product attributes field in the database.



      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties: {}
          application/xml:
            schema:
              type: object
              properties: {}
        description: '**Body Parameters**: `Determined by the category schema`'
    delete:
      summary: Delete Attributes of a Particular Product
      operationId: delete-products-productId-attributes
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Deleted selected product attributes. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - attributes
      description: "**Description:** \n\nThis endpoint pinpoints the product, which is already saved in the system with the help of the product_id provided in the path parameters and then deletes the **attributes of the product** from the database.\n\n\n**Query Parameters**:\n\nnone\n\n**Body Parameters**:\n\nnone"
      security:
        - Authorization: []
  /inventory:
    get:
      summary: Get All items in inventory item
      tags:
        - inventory
      operationId: get-inventory
      description: "**Description:**\n\nThis endpoint returns a list of inventory items, saved in the system by the Administrator/Vendor. \n"
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning List of Inventory Items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Model-Inventory'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
    post:
      summary: Add an inventory item
      operationId: post-inventory
      responses:
        '201':
          description: |-
            ### Accepted

            `Request Accepted:`
            Credential verified. Token generated.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - inventory
      description: |+
        **Description:**

        This endpoint accepts the Inventory Item which is a representation of the stock and type of product inside the inventory.

        It then creates a new entry and adds it into the system.



      security:
        - Authorization: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Model-Inventory'
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|
          |------|------|
          |sku|String|
          |product|String(id)|
          |color|String|
          |total_quantity|Number|
          |available_quantity|Number|
          |blocked_quantity|Number|
  '/inventory/{itemId}':
    parameters:
      - schema:
          type: string
        name: itemId
        in: path
        required: true
    get:
      summary: Get a Particular inventory item
      tags:
        - inventory
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returns A particular inventory item
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Model-Inventory'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-inventory-itemId
      description: "**Description:**\n\nThis endpoint returns a particular inventory items, saved in the system by the Administrator/Vendor. \n"
    put:
      summary: Update a Particular inventory item
      operationId: put-inventory-itemId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected user. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - inventory
      description: |+
        **Description:**

        This endpoint accepts the Inventory Item which is a representation of the stock and type of product inside the inventory.

        It pinpoints the item using the item_id in the path parameters and then updates all the inventory item details.


      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Model-Inventory'
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|
          |------|------|
          |sku|String|
          |product|String(id)|
          |color|String|
          |total_quantity|Number|
          |available_quantity|Number|
          |blocked_quantity|Number|
      security:
        - Authorization: []
    patch:
      summary: Update a Particular inventory item field
      operationId: patch-inventory-itemId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected user details. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - inventory
      description: |+
        **Description:**

        This endpoint accepts the one or more Inventory Item details.

        It then  pinpoints the item using the item_id in the path parameters and then updates all the inventory item details.



      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Model-Inventory'
        description: |-
          **Body Parameters**: One or More of the following

          |body parameter|value type|
          |------|------|
          |sku|String|
          |product|String(id)|
          |color|String|
          |total_quantity|Number|
          |available_quantity|Number|
          |blocked_quantity|Number|
      security:
        - Authorization: []
    delete:
      summary: Delete a Particular inventory item
      operationId: delete-inventory-itemId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Deleted selected user. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - inventory
      description: "**Description:** \n\nThis endpoint pinpoints the inventory item, which is already saved in the system with the help of the item_id provided in the path parameters and Then deletes the item from the database.\n\n\n**Query Parameters**:\n\nnone\n\n**Body Parameters**:\n\nnone"
      security:
        - Authorization: []
  /orders:
    get:
      summary: GET list of orders
      tags:
        - orders
      operationId: get-orders
      description: "**Description:**\n\nThis endpoint returns a list of all order along with the customer, products and shipper details saved in the system by the Administrator/Vendor. \n"
      security:
        - Authorization: []
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returning List of Orders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Model-Order'
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
    post:
      summary: Add/Place an order
      operationId: post-orders
      responses:
        '201':
          description: |-
            ### Created

            `Request Accepted:`
            Order Placed
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '403':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - orders
      description: |
        **Description:**

        This endpoint accepts the Orders for every individual product. It includes both the customer ids, shipper ids and quantity.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Model-Order'
        description: |-
          **Body Parameters**: All of the following

          |body parameter|value type|
          |------|------|
          |product|String(id)|
          |customer|String(id)|
          |shipping_agent|String(id)|
          |quantity|Number|
          |status|String|
          |payment_method|String|
      security:
        - Authorization: []
  '/orders/{orderId}':
    parameters:
      - schema:
          type: string
        name: orderId
        in: path
        required: true
    get:
      summary: Get an Particular Order
      tags:
        - orders
      responses:
        '200':
          description: |-
            ### OK

            `Request Accepted:`
            Returns a particular orders
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Model-Order'
              examples:
                example-1:
                  value:
                    id: string
                    product: string
                    quantity: 0
                    status: Order Placed
                    payment_method: string
                    customer: string
                    shipping_agent: string
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      operationId: get-orders-orderId
      description: "**Description:**\n\nThis endpoint returns a particular order along with the customer, products and shipper details saved in the system by the Administrator/Vendor. "
      security:
        - Authorization: []
    put:
      summary: Update an Particular Order
      operationId: put-orders-orderId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected order. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - orders
      description: |+
        **Description:**

        This endpoint accepts the Orders of an individual product. It includes both the customer ids, shipper ids and quantity.

        It pinpoints the order with the help of the order_id passed through the path parameters.

        Once it recieves the details it finds the order and replaces the all details with the new state.



      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Model-Order'
        description: |
          **Body Parameters**: All of the following

          |body parameter|value type|
          |------|------|
          |product|String(id)|
          |customer|String(id)|
          |shipping_agent|String(id)|
          |quantity|Number|
          |status|String|
          |payment_method|String|
      security:
        - Authorization: []
    patch:
      summary: Update an Particular Order Field
      operationId: patch-orders-orderId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Updated selected order details. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - orders
      description: |
        **Description:**

        This endpoint accepts the Orders of an individual product. It includes both the customer ids, shipper ids and quantity.

        It pinpoints the order with the help of the order_id passed through the path parameters.

        Once it recieves the details it finds the order and updates one or more of the details with the new state.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Model-Order'
        description: |
          **Body Parameters**: One or More of the following

          |body parameter|value type|
          |------|------|
          |product|String(id)|
          |customer|String(id)|
          |shipping_agent|String(id)|
          |quantity|Number|
          |status|String|
          |payment_method|String|
      security:
        - Authorization: []
    delete:
      summary: Delete an Particular Order
      operationId: delete-orders-orderId
      responses:
        '204':
          description: |-
            ### Action Performed
            `Request Accepted:`
            Deleted selected order. No Response has been included for greater responsiveness and speed.
        '400':
          description: |-
            ### Bad Request
            `Reason:`
            The request has been rejected as **parameters/body** does not conform to **expected format**

            `Trouble-shoot:`
            Please recheck if the data is in the correct format.
            Make sure Address field if included has all the fields included.
            Check for spelling mistakes.
        '401':
          description: "### Unauthorized\n`Reason:`\nThe request has been rejected as user was **not authenticated**. This can be because of the following reasons:\n1. Token has **expired**\n2. Token has been **manipulated**\n\n`Trouble-shoot:`\nPlease check if the token has been **manipulated**. If not the token might have expired, if so, **login again** to get new token. "
        '403':
          description: "### Forbidden\n`Reason:`\nThe request has been rejected as user **does not have enough privileges** to **access or modify** this resource\n\n`Trouble-shoot:`\nPlease **upgrade** to an account with a **higher privileges**.\n\nIf you are unable to create an account with a higher privileges, then this resource **must not be accessed/modified** by this account with its **current privileges**. "
        '500':
          description: |
            ### Internal Server Error
            `Reason:`
            We seem to be facing an issue with the backend server.

            `Trouble-shoot:`
            We request you to check the format of the body and parameters once again. If they are valid then *sit tight* as the **development team** will be on it as we speak.
      tags:
        - orders
      description: "**Description:** \n\nThis endpoint pinpoints the order, which is already saved in the system with the help of the order_id provided in the path parameters and then deletes the order from the database.\n\n\n**Query Parameters**:\n\nnone\n\n**Body Parameters**:\n\nnone"
      security:
        - Authorization: []
components:
  schemas:
    Model-Product:
      type: object
      x-examples: {}
      properties:
        id:
          type: string
        product-name:
          type: string
        price:
          type: number
        currency:
          type: string
        vendor:
          $ref: '#/components/schemas/Model-User'
        description:
          type: string
        images:
          type: array
          items:
            type: string
        weight:
          type: string
        size:
          type: string
        mrp:
          type: number
        cost_price:
          type: number
        brand:
          type: string
        color:
          type: string
        category:
          $ref: '#/components/schemas/Model-Category'
      required:
        - id
    Model-Order:
      type: object
      properties:
        id:
          type: string
        product:
          type: string
          description: productId
        quantity:
          type: integer
        status:
          type: string
          enum:
            - Order Placed
            - Picked Up
            - Out For Delivery
            - Delivered
        payment_method:
          type: string
        customer:
          type: string
          description: |
            customerID
        shipping_agent:
          type: string
          description: shipperId-userId
      required:
        - id
    Model-User:
      type: object
      x-examples:
        JSON:
          name: sankalp
          email: sankalp.tvr@gmail.com
          password: password@123
          phone_no: 7397440807
          role: ''
          address:
            address: VIT Chennai
            city: Chennai
            state: Tamil Nadu
            pincode: 600127
      properties:
        id:
          type: string
          readOnly: true
        email:
          type: string
        phone_no:
          type: integer
        password:
          type: string
          writeOnly: true
        role:
          type: string
        address:
          type: object
          required:
            - address
            - city
            - state
            - pincode
          properties:
            address:
              type: string
            city:
              type: string
            state:
              type: string
            pincode:
              type: integer
        name:
          type: string
      required:
        - id
        - email
        - phone_no
        - password
        - role
        - address
        - name
    Model-Inventory:
      type: object
      properties:
        id:
          type: string
        product:
          type: string
          description: |
            productId
        color:
          type: string
        sku:
          type: string
        total_quantity:
          type: integer
        available_quantity:
          type: integer
        blocked_quantity:
          type: integer
        updated_on:
          type: string
      required:
        - id
    Model-Category:
      type: object
      x-tags:
        - tabels
      properties:
        id:
          type: string
        name:
          type: string
        category_schema:
          type: object
    Request_user_post:
      title: Request_user_post
      type: object
      x-examples:
        JSON:
          name: sankalp
          email: sankalp.tvr@gmail.com
          password: password@123
          phone_no: 7397440807
          address:
            address: VIT Chennai
            city: Chennai
            state: Tamil Nadu
            pincode: 600127
      properties:
        name:
          type: string
        email:
          type: string
        password:
          type: string
        phone_no:
          type: integer
        address:
          type: array
          items:
            type: object
            properties:
              address:
                type: string
              city:
                type: string
              state:
                type: string
              pincode:
                type: string
            required:
              - address
              - city
              - state
              - pincode
      required:
        - name
        - email
        - password
        - phone_no
        - address
    Response_user_reg_success:
      title: Register_user_success
      type: object
      properties:
        message:
          type: string
        token:
          type: string
    Request_token_post:
      title: Request_token_post
      type: object
      properties:
        email:
          type: string
        password:
          type: string
      x-examples:
        example-1:
          email: sankalp.tvr@gmail.com
          password: password123
    Response-error:
      title: Response-error
      type: object
      properties:
        error:
          type: string
        suggestion:
          type: string
    Links:
      title: Links
      type: object
      properties:
        href:
          type: string
        method:
          type: string
        rel:
          type: string
    Request_user_post_id:
      title: Request_user_post_id
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        email:
          type: string
        password:
          type: string
        phone_no:
          type: number
        address:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              address:
                type: string
              city:
                type: string
              state:
                type: string
              pincode:
                type: number
        role:
          type: string
    Reques_user_put_optional:
      title: Reques_user_put_optional
      type: object
      properties:
        name:
          type: string
        email:
          type: string
        password:
          type: string
        phone_no:
          type: number
        address:
          type: array
          items:
            type: object
            properties:
              address:
                type: string
              city:
                type: string
              state:
                type: string
              pincode:
                type: number
            required:
              - address
              - city
              - state
              - pincode
        role:
          type: string
    Model-L1-category:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        sub_categories:
          type: array
          items:
            $ref: '#/components/schemas/Model-L2-category'
      required:
        - id
    Model-L2-category:
      type: object
      x-examples: {}
      properties:
        id:
          type: string
        name:
          type: string
        sub_categories:
          type: array
          items:
            $ref: '#/components/schemas/Model-Category'
      required:
        - id
  parameters: {}
  securitySchemes:
    Authorization:
      type: apiKey
      in: header
      name: Authorization
tags:
  - name: register
  - name: login
  - name: user
  - name: user address
  - name: categories
  - name: L2-category
  - name: L1-category
  - name: products
  - name: attributes
  - name: inventory
  - name: orders
